[project]
name = "meta-verify"
authors = ["Your Name <your.email@example.com>"]
description = "A decentralized platform for secure asset verification and tracking"
telemetry = false
requirements = []
[contracts.meta-verify-vault]
path = "contracts/meta-verify-vault.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
